# 🚀 Instrucciones para Subir a GitHub

El repositorio local está listo. Solo los archivos necesarios están incluidos (sin .md, sin node_modules).

## Pasos para subir a GitHub:

### 1. Crear un repositorio en GitHub
- Ve a https://github.com/new
- Nombre: `csrf-demo` (o el que prefieras)
- Descripción: "CSRF Demo - 3 Security Defenses (Vulnerable + Token + SameSite + Headers)"
- Visibilidad: Public (para uso educativo)
- NO inicialices con README (ya tenemos contenido)
- Click en "Create repository"

### 2. Copiar la URL del repositorio
Después de crear, GitHub te mostrará un HTTPS URL como:
```
https://github.com/tu-usuario/csrf-demo.git
```

### 3. Agregar el remote y hacer push
Reemplaza `TU-URL` con la URL que copiaste:

```bash
cd "d:\Octavo Semestre\Desarrollo Seguro\CSRF"

# Agregar el repositorio remoto
git remote add origin TU-URL

# Cambiar rama a main (GitHub usa main por defecto)
git branch -M main

# Subir los archivos
git push -u origin main
```

### Ejemplo completo (si tu usuario es "john"):
```bash
git remote add origin https://github.com/john/csrf-demo.git
git branch -M main
git push -u origin main
```

---

## ✅ Archivos que se suben:

```
✅ package.json
✅ .gitignore
✅ INICIAR-DEMOS.bat
✅ vulnerable/
   ✅ server.js
   ✅ public/index.html
   ✅ usuarios.json
✅ attacker/
   ✅ server.js
   ✅ public/index.html
✅ fixed/
   ✅ server.js
   ✅ public/index.html
   ✅ users.json
   ✅ index.html (navegación visual)
   ✅ defense-1-token/
      ✅ server.js
      ✅ public/index.html
      ✅ usuarios.json
   ✅ defense-2-samesite/
      ✅ strict/server.js, usuarios.json
      ✅ lax/server.js, usuarios.json
      ✅ none/server.js, usuarios.json
      ✅ public/index.html
   ✅ defense-3-headers/
      ✅ server.js
      ✅ public/index.html
      ✅ usuarios.json
```

## ❌ Archivos que NO se suben (excluidos por .gitignore):

```
❌ README.md
❌ GUIA-PRESENTACION.md
❌ fixed/README.md
❌ node_modules/
❌ .env
❌ *.log
```

---

## 📝 Crear un README en GitHub (opcional)

Después de subir, puedes crear un README directamente en GitHub con:

1. Ir a tu repositorio en GitHub
2. Click en "Add file" → "Create new file"
3. Nombre: `README.md`
4. Pega el contenido (puedes copiarlo del archivo local si lo necesitas)

---

## 🔄 Próximas veces que hagas cambios:

```bash
# Hacer cambios en los archivos...

git add .
git commit -m "Descripcion de cambios"
git push
```

---

## ✨ Resultado Final

Tu repositorio en GitHub tendrá:
- ✅ Código limpio y organizado
- ✅ Sin documentación .md (como solicitaste)
- ✅ Sin dependencias (node_modules)
- ✅ Listo para clonar e instalar con `npm install`
- ✅ Perfecto para uso educativo

---

**¡Listo para GitHub!** 🎉
